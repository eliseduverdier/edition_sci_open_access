<% provide(:title, "Reviewing « #{@paper.title} »") %>


Write review for
<h1>
  <%= @paper.title %>
</h1>

<%= render 'form', reviewer: @reviewer %>

<%= link_to 'Show', @reviewer %> |
<%= link_to 'Back', reviewers_path %>
