
<%# TODO provide(:title, "[x notifs]") %>

<%=
if logged_in?

    if current_user.is_researcher?
      render 'static_pages/home_researcher'
    elsif current_user.is_admin?
      render 'static_pages/home_admin'
    elsif current_user.is_editor?
      render 'static_pages/home_editor'
    else
      render 'static_pages/home_reader'
   end

else

  render 'static_pages/home'

end
%>
